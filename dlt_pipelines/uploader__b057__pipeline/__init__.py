"""Compatibility package for the legacy uploader B057 import path.

The canonical implementation lives in ``internal.dltaf_plugins`` inside the
consumer repository. This package stays in OSS only to preserve package-mode
import compatibility during the cutover away from ``dlt_pipelines``.
"""

