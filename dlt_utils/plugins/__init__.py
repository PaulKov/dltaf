"""Plugin namespaces for the framework.

This package is intentionally small and optional.

- Core framework supports plugin loading for:
  - infra checks (Stage 18)
  - hooks + runners (Stage 24)

The subpackage `dlt_utils.plugins.examples` contains tiny examples used in tests
and as a reference for future integrations.
"""
